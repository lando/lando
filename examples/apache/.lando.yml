name: lando-apache
proxy:
  custom:
    - landoapache.lndo.site
events:
  post-start:
    - custom: fuck off
services:
  defaults:
    type: apache
  patch:
    type: apache:2.4.39
  custom:
    type: apache
    webroot: web
    ssl: true
    config:
      server: config/httpd.conf
      vhosts: config/custom.conf
  override:
    type: apache
    webroot: web
