name: lando-tomcat
services:
  custom:
    type: tomcat
    ssl: true
    webroot: .
    # config:
    #   server: config/web.xml
    #   web: config/web.xml
    #   context: config/context.xml
    #   users: config/tomcat-users.xml
