name: trivial-services
sharing:
  data:
    local: .
    remote: /usr/share/nginx/html
proxy:
  appserver:
    - port: 80/tcp
      default: true
    - port: 443/tcp
      secure: true
      default: true
services:
  appserver:
    type: nginx:1.11
    ssl: true
    config:
      server: config/server.conf
