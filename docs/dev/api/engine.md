<a name="engine"></a>

## engine : <code>object</code>
Things Things Things Things Things Things Things Things Things Things
Things Things Things Things Things Things
Things Things Things Things Things Things Things Things
Things Things Things Things Things Things

**Kind**: global namespace  
**Emits**: <code>event:pre-bootstrap</code>  

* [engine](#engine) : <code>object</code>
    * [.up](#engine.up) ⇒ <code>Array</code>
    * [.isUp](#engine.isUp) ⇒ <code>Array</code>
    * [.down](#engine.down) ⇒ <code>Array</code>
    * [.isRunning](#engine.isRunning) ⇒ <code>Array</code>
    * [.list](#engine.list) ⇒ <code>Array</code>
    * [.exists](#engine.exists) ⇒ <code>Array</code>
    * [.inspect](#engine.inspect) ⇒ <code>Array</code>
    * [.start](#engine.start) ⇒ <code>Array</code>
    * [.run](#engine.run) ⇒ <code>Array</code>
    * [.stop](#engine.stop) ⇒ <code>Array</code>
    * [.destroy](#engine.destroy) ⇒ <code>Array</code>
    * [.build](#engine.build) ⇒ <code>Array</code>

<a name="engine.up"></a>

### engine.up ⇒ <code>Array</code>
Lists all the Lando apps

**Kind**: static namespace of [<code>engine</code>](#engine)  
**Returns**: <code>Array</code> - Returns the total.  
**Since**: 3.0.0  

| Param | Type | Description |
| --- | --- | --- |
| [opts] | <code>Object</code> | Things |

**Example**  
```js
// List all the apps
return lando.app.list()

// Map each app to a summary and print results
.map(function(app) {
 return appSummary(app)
  .then(function(summary) {
   console.log(JSON.stringify(summary, null, 2));
 });
});
```
<a name="engine.isUp"></a>

### engine.isUp ⇒ <code>Array</code>
Lists all the Lando apps

**Kind**: static namespace of [<code>engine</code>](#engine)  
**Returns**: <code>Array</code> - Returns the total.  
**Since**: 3.0.0  

| Param | Type | Description |
| --- | --- | --- |
| [opts] | <code>Object</code> | Things |

**Example**  
```js
// List all the apps
return lando.app.list()

// Map each app to a summary and print results
.map(function(app) {
 return appSummary(app)
  .then(function(summary) {
   console.log(JSON.stringify(summary, null, 2));
 });
});
```
<a name="engine.down"></a>

### engine.down ⇒ <code>Array</code>
Lists all the Lando apps

**Kind**: static namespace of [<code>engine</code>](#engine)  
**Returns**: <code>Array</code> - Returns the total.  
**Since**: 3.0.0  

| Param | Type | Description |
| --- | --- | --- |
| [opts] | <code>Object</code> | Things |

**Example**  
```js
// List all the apps
return lando.app.list()

// Map each app to a summary and print results
.map(function(app) {
 return appSummary(app)
  .then(function(summary) {
   console.log(JSON.stringify(summary, null, 2));
 });
});
```
<a name="engine.isRunning"></a>

### engine.isRunning ⇒ <code>Array</code>
Lists all the Lando apps

**Kind**: static namespace of [<code>engine</code>](#engine)  
**Returns**: <code>Array</code> - Returns the total.  
**Since**: 3.0.0  

| Param | Type | Description |
| --- | --- | --- |
| [opts] | <code>Object</code> | Things |

**Example**  
```js
// List all the apps
return lando.app.list()

// Map each app to a summary and print results
.map(function(app) {
 return appSummary(app)
  .then(function(summary) {
   console.log(JSON.stringify(summary, null, 2));
 });
});
```
<a name="engine.list"></a>

### engine.list ⇒ <code>Array</code>
Lists all the Lando apps

**Kind**: static namespace of [<code>engine</code>](#engine)  
**Returns**: <code>Array</code> - Returns the total.  
**Since**: 3.0.0  

| Param | Type | Description |
| --- | --- | --- |
| [opts] | <code>Object</code> | Things |

**Example**  
```js
// List all the apps
return lando.app.list()

// Map each app to a summary and print results
.map(function(app) {
 return appSummary(app)
  .then(function(summary) {
   console.log(JSON.stringify(summary, null, 2));
 });
});
```
<a name="engine.exists"></a>

### engine.exists ⇒ <code>Array</code>
Lists all the Lando apps

**Kind**: static namespace of [<code>engine</code>](#engine)  
**Returns**: <code>Array</code> - Returns the total.  
**Since**: 3.0.0  

| Param | Type | Description |
| --- | --- | --- |
| [opts] | <code>Object</code> | Things |

**Example**  
```js
// List all the apps
return lando.app.list()

// Map each app to a summary and print results
.map(function(app) {
 return appSummary(app)
  .then(function(summary) {
   console.log(JSON.stringify(summary, null, 2));
 });
});
```
<a name="engine.inspect"></a>

### engine.inspect ⇒ <code>Array</code>
Lists all the Lando apps

**Kind**: static namespace of [<code>engine</code>](#engine)  
**Returns**: <code>Array</code> - Returns the total.  
**Since**: 3.0.0  

| Param | Type | Description |
| --- | --- | --- |
| [opts] | <code>Object</code> | Things |

**Example**  
```js
// List all the apps
return lando.app.list()

// Map each app to a summary and print results
.map(function(app) {
 return appSummary(app)
  .then(function(summary) {
   console.log(JSON.stringify(summary, null, 2));
 });
});
```
<a name="engine.start"></a>

### engine.start ⇒ <code>Array</code>
Lists all the Lando apps

**Kind**: static namespace of [<code>engine</code>](#engine)  
**Returns**: <code>Array</code> - Returns the total.  
**Since**: 3.0.0  

| Param | Type | Description |
| --- | --- | --- |
| [opts] | <code>Object</code> | Things |

**Example**  
```js
// List all the apps
return lando.app.list()

// Map each app to a summary and print results
.map(function(app) {
 return appSummary(app)
  .then(function(summary) {
   console.log(JSON.stringify(summary, null, 2));
 });
});
```
<a name="engine.run"></a>

### engine.run ⇒ <code>Array</code>
Lists all the Lando apps

**Kind**: static namespace of [<code>engine</code>](#engine)  
**Returns**: <code>Array</code> - Returns the total.  
**Since**: 3.0.0  

| Param | Type | Description |
| --- | --- | --- |
| [opts] | <code>Object</code> | Things |

**Example**  
```js
// List all the apps
return lando.app.list()

// Map each app to a summary and print results
.map(function(app) {
 return appSummary(app)
  .then(function(summary) {
   console.log(JSON.stringify(summary, null, 2));
 });
});
```
<a name="engine.stop"></a>

### engine.stop ⇒ <code>Array</code>
Lists all the Lando apps

**Kind**: static namespace of [<code>engine</code>](#engine)  
**Returns**: <code>Array</code> - Returns the total.  
**Since**: 3.0.0  

| Param | Type | Description |
| --- | --- | --- |
| [opts] | <code>Object</code> | Things |

**Example**  
```js
// List all the apps
return lando.app.list()

// Map each app to a summary and print results
.map(function(app) {
 return appSummary(app)
  .then(function(summary) {
   console.log(JSON.stringify(summary, null, 2));
 });
});
```
<a name="engine.destroy"></a>

### engine.destroy ⇒ <code>Array</code>
Lists all the Lando apps

**Kind**: static namespace of [<code>engine</code>](#engine)  
**Returns**: <code>Array</code> - Returns the total.  
**Since**: 3.0.0  

| Param | Type | Description |
| --- | --- | --- |
| [opts] | <code>Object</code> | Things |

**Example**  
```js
// List all the apps
return lando.app.list()

// Map each app to a summary and print results
.map(function(app) {
 return appSummary(app)
  .then(function(summary) {
   console.log(JSON.stringify(summary, null, 2));
 });
});
```
<a name="engine.build"></a>

### engine.build ⇒ <code>Array</code>
Lists all the Lando apps

**Kind**: static namespace of [<code>engine</code>](#engine)  
**Returns**: <code>Array</code> - Returns the total.  
**Since**: 3.0.0  

| Param | Type | Description |
| --- | --- | --- |
| [opts] | <code>Object</code> | Things |

**Example**  
```js
// List all the apps
return lando.app.list()

// Map each app to a summary and print results
.map(function(app) {
 return appSummary(app)
  .then(function(summary) {
   console.log(JSON.stringify(summary, null, 2));
 });
});
```
